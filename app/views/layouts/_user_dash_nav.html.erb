<div class="container-fluid dash-container" style="padding:0">
    <ul class="dashnav">
        <li>
            <% if controller_name=="user_panel" and action_name=="index" %>
                <a class="active" href="/user_panel">Dashboard</a>
            <% else %>
                <a href="/user_panel">Dashboard</a>
            <% end %>
        </li>
        <li>
            <% if controller_name=="user_panel" and action_name=="payments" %>
                <a class="active" href="/user_panel/payments">Payments</a>
            <% else %>
                <a href="/user_panel/payments">Payments</a>
            <% end %>
        </li>
        <li>
            <% ujam = current_user.jobs_as_employer.where(status: "PENDING").count %>
            <% if controller_name=="user_panel" and action_name=="jobs_as_employer" %>
                <a class="active" href="/jobs_as_employer">Jobs as employer<span class="badge badge-danger"><%=  ujam %> </span></a>
            <% else %>
                <a href="/jobs_as_employer">Jobs as employer<span class="badge badge-danger"><%= ujam %> </span></a>
            <% end %>
        </li>
            
            <% ujaw = current_user.jobs_as_worker.where(status: "RESERVED").count %>
        <li>
            <% if controller_name=="user_panel" and action_name=="jobs_as_worker" %>
                <a class="active" href="/jobs_as_worker">Jobs as worker <span class="badge badge-danger"><%=  ujaw %> </span></a>
            <% else %>
                <a href="/jobs_as_worker">Jobs as worker <span class="badge badge-danger"><%= ujaw %> </span></a>
            <% end %>
        </li>
        
        <% urae = current_user.reservations_as_employer.where(status: "PENDING").count %>
        <li>
            <% if controller_name=="user_panel" and action_name=="reservations_as_employer" %>
                <a class="active" href="/reservations_as_employer">Reservations as Employer <span class="badge badge-danger"><%=  urae %> </span></a>
            <% else %>
                <a href="/reservations_as_employer">Reservations as Employer <span class="badge badge-danger"><%= urae %> </span></a>
            <% end %>
        </li>
        
        
        <% uraw = current_user.reservations_as_worker.where(status: "PENDING").count %>
        <li>
            <% if controller_name=="user_panel" and action_name=="reservations_as_worker" %>
                <a class="active" href="/reservations_as_worker">Reservations as Worker <span class="badge badge-danger"><%=  uraw %> </span></a>
            <% else %>
                <a href="/reservations_as_worker">Reservations as Worker <span class="badge badge-danger"><%= uraw %> </span></a>
            <% end %>
        </li>
    </ul>
</div>