<div class="container-fluid dash-container" style="padding:0">
  <div class="navbar-header d-lg-none">
    <ul class="navbar-nav services" style="width:100%">
      <li>
         <% Category.all.each do |category| %>
        <li class="dropdown show">  
        <a class="dropdown-toggle" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" href="<%= main_app.jobs_path(category: category.name)%>" data-toggle="dropdown"><%= category.name %></a>
        
  			<div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
  			  <% category.subcategories.all.each do |subcategory| %>
    				<a class="dropdown-item" href="<%= main_app.jobs_path(subcategory: subcategory.name) %>"><%= subcategory.name %></a>
    			<% end %>
  			</div>
  			</li>
        <% end %>
      </li>
    </ul>
    </div>
  <ul class="nav dashnav d-none d-lg-inline-block">
    <!--<li><= ((link_to "All Jobs", main_app.jobs_path,class: "active") if @status == "alljobs") || (link_to "All Jobs", main_app.jobs_path)  %></li>-->
    
    <% Category.all.each do |category| %>
    
      <!--<li><= ((link_to category.name, main_app.jobs_path(category: category.name),class:"active") if @status == category.name) || (link_to category.name, main_app.jobs_path(category: category.name)) %></li>-->
      
      <!--SUBCATEGORIES-->
      
      <!--< category.subcategories.all.each do |subcategory| %>-->
        <!--<li><= link_to subcategory.name, main_app.jobs_path(subcategory: subcategory.name)%></li>  -->
      <!--< end %>-->
      <li class="dropdown show">  
      <a class="dropdown-toggle" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" href="<%= main_app.jobs_path(category: category.name)%>" data-toggle="dropdown"><%= category.name %></a>
      
			<div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
			  <% category.subcategories.all.each do |subcategory| %>
  				<a class="dropdown-item" href="<%= main_app.jobs_path(subcategory: subcategory.name) %>"><%= subcategory.name %></a>
  			<% end %>
			</div>
			</li>
      
      
      
      
    <% end %>
    
  </ul>
</div>