<!DOCTYPE html>
<html>
  <head>
    <title>Freelance</title>
    <%= csrf_meta_tags %>
    <link href="https://use.fontawesome.com/releases/v5.0.7/css/all.css" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js"></script>
    <script src="https://npmcdn.com/tether@1.2.4/dist/js/tether.min.js"></script>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <meta name= viewport content= width=device-width initial-scale=1>
  </head>

  <body>
    <%# unless params[:controller]=='home' && params[:action]=='admin' %>
      <div class="body">
          <%= render 'layouts/header' unless params[:controller]=='home' && params[:action]=='admin' %>
          <% if notice || alert %>
            <div class="alert alert-info" role="alert">
              <strong>Info!</strong> <%= notice || alert %>
            </div>
          <% end %>
          
          <div class="container-fluid main" style="background: #f5f5f5; padding:0;">
          <div role="main" class="main" style="min-height:82.5vh;">

          <%# unless params[:controller]=='home' && params[:action]=='admin' %>
          
          <%# end %> 
            <% if user_signed_in? %>
              <%= render ( ("layouts/admin_dash_nav" if current_user.is_admin?) || "layouts/user_dash_nav") %>
            <% end %>
            <%= yield %>
          <%# unless params[:controller]=='home' && params[:action]=='admin' %>
                 
          <%# end %>
          </div>
          </div>
          <%= render 'layouts/footer' unless params[:controller]=='home' && params[:action]=='admin' %>
      </div> 
  </body>
</html>
